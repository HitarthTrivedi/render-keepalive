name: Keep Render Awake

on:
  schedule:
    - cron: "*/2 * * * *"   # runs every 5 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping website
        run: curl -I https://threef1.onrender.com
